name: agent_protocol
description: Wire protocol for voice-agents-hack-flutter (IntentRequest / IntentResponse / ToolCallTrace).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  uuid: ^4.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
