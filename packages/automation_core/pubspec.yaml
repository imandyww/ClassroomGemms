name: automation_core
description: macOS automation tool surface (vendored/adapted from NextDesk) for voice-agents-hack-flutter.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  bixat_key_mouse: 1.0.3
  screen_capturer: ^0.2.1
  image: ^4.5.4

dev_dependencies:
  flutter_lints: ^6.0.0
