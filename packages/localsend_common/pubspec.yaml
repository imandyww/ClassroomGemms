name: localsend_common
description: Vendored LocalSend protocol DTOs, constants, and discovery for voice-agents-hack-flutter.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  collection: ^1.17.2
  dart_mappable: ^4.2.0
  logging: ^1.2.0
  meta: ^1.9.1
  mime: ^1.0.4
  refena: ^3.1.0

dev_dependencies:
  build_runner: ^2.4.7
  dart_mappable_builder: ^4.2.0
  lints: ^6.0.0
  test: ^1.21.0
